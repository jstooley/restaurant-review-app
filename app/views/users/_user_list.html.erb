<div>
  <ul>
    <% @users.each do |user| %>
      <% unless current_user == user %>
        <li><%= link_to "#{user.username}'s reviews" , user_reviews_path(user.id) %></li>
      <% end %>
    <% end %>
  <ul>
</div>