<h1><%= @user.username %>'s Reviews</h1>
<ul>
<% @reviews.each do |review| %>
    <li>
      <%= link_to "#{review.title} -  #{review.restaurant.name}", user_review_path(@user.id, review.id) %>
    </li>
<% end %>
</ul>