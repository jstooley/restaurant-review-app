<% if review.user_id == current_user.id %>
  <%= link_to "Edit Review?", edit_user_review_path(review.id) %>
  <%= link_to "Destroy Review?", review,method: :delete,  data:{confirm: "Are you sure?"}%>
<% end %>